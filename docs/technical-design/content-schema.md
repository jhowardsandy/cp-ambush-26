# Content Schema

Data-driven scenario and content schemas are deferred. Milestone 1 serializes minimum replay data with `System.Text.Json`; IDs and version fields are retained for future schema validation and compatibility work.
